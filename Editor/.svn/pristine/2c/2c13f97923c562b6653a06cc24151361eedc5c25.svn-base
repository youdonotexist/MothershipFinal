/*
 * Copyright 2013 SirsiDynix.  All rights reserved.
 */
Utils = {};

Utils.sanitizeValue = function(value, defaultValue) {
	if (value && !isNaN(value)) {
		return value;
	}

	return defaultValue;
}